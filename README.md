# top-k-yelp-recommendation-system
A query-driven top-k recommendation system built on the Yelp dataset, combining structured attributes (ratings) and unstructured text (reviews) using a custom ranking algorithm.
